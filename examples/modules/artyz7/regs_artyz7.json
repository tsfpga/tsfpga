{
  "dummy_regs": {
    "array_length": 4,
    "registers": {
      "configuration": {
        "description": "Registers are R/W so they won't be optimized away",
        "mode": "r_w",
        "bits": {
          "enable": "",
          "disable": ""
        }
      },
      "settings": {
        "description": "Registers are R/W so they won't be optimized away",
        "mode": "r_w"
      }
    }
  },
  "plain_dummy_reg": {
    "mode": "r_w"
  }
}